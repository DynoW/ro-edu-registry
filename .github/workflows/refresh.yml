# name: Weekly feed refresh

# on:
#   schedule:
#     - cron: '0 3 * * 1' # Mondays 03:00 UTC
#   workflow_dispatch:

# permissions:
#   contents: read
#   issues: write

# concurrency:
#   group: refresh
#   cancel-in-progress: false

# jobs:
#   refresh:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'
#       - run: pip install httpx
#       - name: Re-scrape admitere.edu.ro feeds
#         run: python3 scripts/scraper.py
#       - name: Compare live feeds against canonical registry
#         run: python3 scripts/refresh_check.py
#       - name: Open or update the feed-drift issue
#         if: hashFiles('public/data/refresh_report.md') != ''
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const fs = require('fs');
#             const title = '📡 Weekly feed refresh: admitere.edu.ro drifted';
#             const body = fs.readFileSync('public/data/refresh_report.md', 'utf8');
#             const existing = await github.rest.issues.listForRepo({
#               owner: context.repo.owner, repo: context.repo.repo,
#               state: 'open', labels: 'feed-refresh', per_page: 1,
#             });
#             if (existing.data.length) {
#               await github.rest.issues.update({
#                 owner: context.repo.owner, repo: context.repo.repo,
#                 issue_number: existing.data[0].number, title, body,
#               });
#             } else {
#               await github.rest.issues.create({
#                 owner: context.repo.owner, repo: context.repo.repo,
#                 title, body, labels: ['feed-refresh'],
#               });
#             }
#       - uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name: refresh-report
#           path: public/data/
#           retention-days: 30
